<!-- Composant pour afficher "une boutique à taille humaine" -->
<article class="human-shop">
    <div class="container">
        <header>
            <h2>Une boutique à taille <br> humaine</h2>
        </header>
        <div class="content">
            <div class="image-wrapper">
                <img 
                    src="/assets/dons.jpg" 
                    alt="Deux personnes manipulant une boîte de dons, illustrant le travail d'équipe dans la boutique" 
                    class="shop-image"
                />
            </div>
            <div class="text-content">
                <p>
                    Notre boutique s'appuie sur une gouvernance structurée, alliant professionnels et bénévoles. Sous la direction d'une équipe de direction engagée, tous oeuvrent ensemble dans un même but : créer un espace chaleureux et accessible à tous. Cette collaboration étroite entre équipes salariées et bénévoles fait de notre boutique un véritable lieu de vie et de rencontre.
                </p>
            </div>
        </div>
    </div>
</article>

<style>
    .human-shop {
        padding: clamp(2rem, 6vh, 4rem) 0;
        background-color: #ffffff;
    }

    .container {
        max-width: 1400px;
        margin: 0 auto;
        padding: 0 clamp(1rem, 3vw, 2rem);
    }

    h2 {
        position: relative;
        text-align: left;
        left: 2%;
        color: #D52151;
        font-size: clamp(2rem, 5vw, 3.5rem);
        font-weight: 100;
        width: fit-content;
        margin: clamp(30px, 5vh, 50px) 0 clamp(2rem, 5vh, 4rem);
        padding-bottom: 1rem;
        border-bottom: 2px solid currentColor;
    }

    .content {
        display: flex;
        justify-content: space-between;
        align-items: center;
        gap: clamp(2rem, 4vw, 4rem);
        padding: 0 clamp(1rem, 2vw, 2rem);
        max-width: 1400px;
        margin: 0 auto;
    }

    .image-wrapper {
        flex: 1;
        max-width: 50%;
        border-radius: clamp(0.5rem, 1vw, 1rem);
        overflow: hidden;
    }

    .shop-image {
        width: 100%;
        height: auto;
        object-fit: cover;
        display: block;
        transition: transform 0.3s ease;
    }

    .text-content {
        flex: 1;
        position: relative;
        max-width: 600px;
    }

    .text-content p {
        font-size: clamp(1.2rem, 2vw, 1.5rem);
        line-height: 1.3;
        margin-bottom: 1.5rem;
        font-weight: 300;
    }

    /* Responsive */
    @media (max-width: 1200px) {
        h2 {
            right: 19%;
        }
    }

    @media (max-width: 1024px) {
        h2 {
            text-align: center;
            margin: clamp(30px, 5vh, 50px) auto clamp(2rem, 5vh, 4rem);
        }
    }

    @media (max-width: 768px) {
        h2 {
            text-align: center;
            margin: clamp(20px, 4vh, 30px) auto clamp(1rem, 4vh, 2rem);
            font-size: clamp(1.8rem, 6vw, 2.2rem);
        }

        .content {
            flex-direction: column;
            gap: clamp(1.5rem, 4vh, 2rem);
            padding: 0 clamp(0.5rem, 2vw, 1rem);
        }

        .image-wrapper {
            max-width: 100%;
        }

        .text-content {
            left: 0;
            text-align: center;
            max-width: 100%;
            padding: 0 clamp(1rem, 4vw, 2rem);
        }
    }
</style>